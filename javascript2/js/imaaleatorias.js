let img = document.createElement("img"), //esto crea una etiqueta en este caso la etiqueta img
    divimagenes = document.queryselector('#n2'),
    num = Math.round(Math.round() * (10 - 1) + 1);

function buscarImagen() {
    img.src = ` autos/${1}.jpg `;
    divimagenes.append(img);

}